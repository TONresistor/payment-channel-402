{
  "name": "pc402-cli",
  "version": "0.1.0",
  "description": "CLI for pc402 payment channels — fetch, channel management, wallet",
  "type": "module",
  "main": "./dist/index.js",
  "bin": {
    "pc402": "./dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "dev": "tsup --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "pc402-core": "*",
    "pc402-fetch": "*",
    "commander": "^13.1.0",
    "@ton/ton": ">=15.0.0"
  },
  "peerDependencies": {
    "@ton/core": ">=0.60.0",
    "@ton/crypto": ">=3.3.0"
  },
  "devDependencies": {
    "tsup": "^8.4.0",
    "typescript": "^5.8.3"
  }
}
